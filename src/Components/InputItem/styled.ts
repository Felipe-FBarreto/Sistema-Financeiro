import styled from "styled-components";

export const Wrapper = styled.div`

  label{
    font-weight: bold;
    font-size: 1.2rem;
  }

  input{
    display: block;
    border-radius: 5px;
    border: none;
    border: 1px solid #ccc;
    outline: none;
    padding: .5rem;
    font-size: .8rem;
  }

`;

